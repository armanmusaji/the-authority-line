/** How long each step of Play the night stays before the next. Slow enough to hear through a screen reader. */
export const PLAY_INTERVAL_MS = 2500

/** The phone layout's breakpoint, matching the max-width: 760px rules in styles.css. */
export const PHONE_QUERY = '(max-width: 760px)'

/** How long the settings group stays outlined after "Change this setting". */
export const HIGHLIGHT_MS = 1800
